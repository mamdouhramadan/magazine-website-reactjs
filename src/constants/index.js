import { colors } from "./colors"

// export const logo = require('./../assets/logo.png');

export const theme = {
    dark: {
        main: colors.black,
        text: colors.white,
        light: colors.gray,
        invert: colors.white

    },
    light: {
        main: colors.white,
        text: colors.black,
        light: colors.light,
        invert: colors.black
    }
}  